# Build uses the pre-built `ui/data/rooms.js` + `ui/maps/*.png` assets
# committed to the mapper repo (generated by `scripts/build-room-db.mjs`).

withdrawn = true
withdrawn_reason = "Unmaintained — no longer recommended for new installs."

[source]
repo     = "wizardquack/mallardx-discworld-mapper"
ref      = "v0.1.3"
build    = "rm -f discworld-mapper.mallardx && zip -qr discworld-mapper.mallardx plugin.toml src ui README.md"
artifact = "discworld-mapper.mallardx"

[meta]
homepage = "https://github.com/wizardquack/mallardx-discworld-mapper"
tags     = ["discworld", "mapper", "panel"]
